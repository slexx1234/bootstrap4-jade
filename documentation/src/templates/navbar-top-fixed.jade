extends ../../../library/_master

block title
    title Fixed top navbar example for Bootstrap

append css
    +css('http://v4-alpha.getbootstrap.com/examples/navbar-top-fixed/navbar-top-fixed')

block container
    .pos-f-t
        +navbar-collapse('navbar-header')
            .container-fluid.bg-inverse.p-a-1
                h3 Collapsed content
                p Toggleable via the navbar brand.

        +navbar('faded').navbar-full
            +navbar-toggler('navbar-header')

    .container
        +jumbotron
            h1 Navbar example
            p.lead.
                This example is a quick exercise to illustrate how fixed to top navbar
                works. As you scroll, it will remain fixed to the top of your browser's viewport.

            +btn('primary', 'lg') View navbar docs »
